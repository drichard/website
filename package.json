{
  "name": "website",
  "version": "1.0.0",
  "scripts": {
    "update": "cd mindmaps-src && git pull && cd ..",
    "build": "rm -rf ./mindmaps && mkdir mindmaps && cd mindmaps-src && npm run build && cd .. && cp -r mindmaps-src/dist/* mindmaps",
    "deploy": "git add . && git commit -m 'Deploy mindmaps' && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drichard/website.git"
  },
  "author": "David Richard",
  "license": "ISC"
}
